#include "main.h"

/**
 * _strcat - concatennates two strings
 * @dest: copy to
 * @src: copy from
 * Return: pointer to dest
 */
char*_stract(char*dest, const char*src)
{
	int index = 0, dest_len = 0;

		int index = 0, dest_len + 0;

		while (dest[index++]
			dest_len++;

		for (index = 0; src[index]; index++)
			dest[dest_len++] = src[index];

		return (dest);
}
